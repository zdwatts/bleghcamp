1. Set up GitHub repo
2. Set up database with .sequelizerc
3. Create .env in backend directory, populate with user and jwt data
  a. port defaults to 5000
4. create a .gitignore in top-level directory
	a. specifications
		1. backend/.env
		2. frontend/.env
		3. backend/node_modules/
		4. frontend/node_modules/
5. verify database credentials in .env connect to a database in postgres
6. use sequelize-cli commands to run migrations and seeds
